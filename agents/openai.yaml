interface:
  display_name: "Boosta API Video Creator"
  short_description: "Create and track Boosta API video jobs"
  default_prompt: "Use $boosta-video-api to submit a Boosta job, poll status, and return clip URLs."
